apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

configMapGenerator:
  - name: train-config
    envs:
      - backend-config.env

generatorOptions:
  disableNameSuffixHash: true
